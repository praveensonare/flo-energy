version: "3.9"

# ============================================================
# Flo Energy – Meter Reading ETL Agent  – Local Dev Stack
# ============================================================
# Services:
#   postgres  – local PostgreSQL 15 instance
#   agent     – the LangGraph ETL agent (requires ANTHROPIC_API_KEY)
#
# Usage:
#   docker compose up postgres -d
#   docker compose run agent "/data/nem12#assessment001#UNITEDDP#NEMMCO.csv"
#   docker compose run agent "/data/nem12#assessment001#UNITEDDP#NEMMCO.csv" "/data/nem12#0123456789ABCDEF#TESTMDP1#TESTRETAIL.zip"

services:

  # ----------------------------------------------------------
  # PostgreSQL 15
  # ----------------------------------------------------------
  postgres:
    image: postgres:15-alpine
    container_name: meter_postgres
    environment:
      POSTGRES_DB: meter_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d meter_db"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  # ----------------------------------------------------------
  # ETL Agent
  # ----------------------------------------------------------
  agent:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    container_name: meter_agent
    depends_on:
      postgres:
        condition: service_healthy
    # All settings come from .env — pydantic-settings reads it at startup.
    # DATABASE_URL is the only override: inside the container the Postgres
    # host is the service name "postgres", not "localhost".
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/meter_db
    volumes:
      - ./data:/data:ro          # input files (read-only)
      - ./output:/output         # SQL output (writable)
    ports:
      - "8000:8000"              # Prometheus metrics
    # Default: run the agent against the assessment sample file (override with docker compose run)
    command: ["/data/nem12#assessment001#UNITEDDP#NEMMCO.csv"]
    restart: "no"

volumes:
  postgres_data:
